This is an implementation of Gaussian elimination written to practice my Python skills.

It contains functions for computing the row-echelon form (REF) and reduced row-echelon form (RREF) of real-valued matrices.

The implementation includes partial pivoting and a numerical tolerance for handling floating-point errors.

Example and test files are also included.